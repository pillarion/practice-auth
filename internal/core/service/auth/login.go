package auth

import (
	"context"

	model "github.com/pillarion/practice-auth/internal/core/model/auth"
)

func (s *service) Login(ctx context.Context, cred model.Credential) (string, error) {

	return "", nil
}
